spring:
  application:
    name : "TelegramBot"
  datasource:
    driver_class_name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:4321/TraderDB
    username: postgres
    password: postgres
  liquibase:
    enabled: false
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: telegram-notifier-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.value.default.type: org.example.postgres.entity.TradeDecisionEntity

app:
  telegram-token: "7424392616:AAEKAIDNyybDZ_DE8PcNxAWN8NJggvaDgXA"
server:
  port: 8888
kafka:
  topic:
    name: tradeResponse