services:
  postgres_db:
    container_name: postgres
    image: postgres:17.0-alpine3.20
    environment:
      POSTGRES_DB: "TraderDB"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      PGDATA: "/var/lib/postgresql/data/pg-volume"
    ports:
      - "4321:5432"
    volumes:
      - pg-volume:/var/lib/postgresql/data
    networks:
      - app-network

  zookeeper:
    image: bitnami/zookeeper:3.9
    container_name: zookeeper
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    networks:
      - app-network
    volumes:
      - zookeeper_data:/bitnami/zookeeper
    restart: always

    # Сервис Kafka
  kafka:
    image: bitnami/kafka:3.7
    container_name: kafka
    ports:
      - "9092:9092"
      - "9093:9093" # Для внутренней коммуникации
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://26.236.88.200:9092,PLAINTEXT_HOST://localhost:9093
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      - KAFKA_CFG_LISTENERS=PLAINTEXT://0.0.0.0:9092,PLAINTEXT_HOST://0.0.0.0:9093
      - ALLOW_PLAINTEXT_LISTENER=yes
      - KAFKA_CFG_LOG_RETENTION_MS=1000          # Сообщения удаляются через 1 секунду
      - KAFKA_CFG_LOG_SEGMENT_DELETE_DELAY_MS=500 # Сегменты удаляются через 0.5 секунд
      - KAFKA_CFG_LOG_CLEANUP_POLICY=delete       # Политика очистки - удаление
    depends_on:
      - zookeeper
    networks:
      - app-network
    volumes:
      - kafka_data:/bitnami/kafka
    restart: always

volumes:
  pg-volume:
  zookeeper_data:
  kafka_data:

networks:
  app-network:
    driver: bridge